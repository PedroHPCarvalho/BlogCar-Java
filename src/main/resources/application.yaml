spring:
  application:
    name: principalblog
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/dev_carblog
    username: postgres
    password: dokidoki01
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8080